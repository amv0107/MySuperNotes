<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MySuperNotes" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">?attr/backgroundAppFromTheme</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorControlNormal">@color/grey</item>
        <item name="backgroundCardFromTheme">@color/black</item>
        <item name="backgroundAppFromTheme">@color/black</item>
    </style>

    <style name="Theme.MySuperNotes" parent="Base.Theme.MySuperNotes" />
</resources>